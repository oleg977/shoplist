# 🛒 ShopList: Витрина товаров для розничной торговли

Сайт для локальных сетей розничной торговли, позволяющий публиковать ассортимент, цены и адреса магазинов для удобного просмотра на мобильных устройствах.

## 🚀 1. Стек технологий

* **Backend:** Django 5.2.7
* **Database:** PostgreSQL (Production), SQLite (Development)
* **Hosting:** Render.com (Production-ready)
* **UI/Frontend:** Bootstrap 5 (Адаптивный дизайн)
* **Environment:** Gunicorn, WhiteNoise, `python-dotenv`

## 📦 2. Установка и запуск проекта

### 2.1. Требования

* Python 3.10+
* Git

### 2.2. Установка

1.  **Клонирование репозитория:**
    ```bash
    git clone [https://github.com/ВАШ_НИК/shoplist.git](https://github.com/ВАШ_НИК/shoplist.git)
    cd shoplist
    ```

2.  **Создание и активация виртуального окружения:**
    ```bash
    python -m venv .venv
    # Windows
    .\.venv\Scripts\activate
    # macOS/Linux
    source .venv/bin/activate
    ```

3.  **Установка зависимостей:**
    ```bash
    pip install -r requirements.txt
    ```

4.  **Настройка переменных окружения:**
    Создайте файл `.env` в корне проекта и добавьте базовые переменные:
    ```
    # .env
    SECRET_KEY='ваш_секретный_ключ'
    DEBUG=True
    ALLOWED_HOSTS=127.0.0.1,localhost
    ```

5.  **Миграции и создание суперпользователя:**
    ```bash
    python manage.py makemigrations users products 
    python manage.py migrate
    python manage.py createsuperuser 
    ```

### 2.3. Запуск

```bash
python manage.py runserver
Отлично! Ваш проект полностью рабочий и протестирован. Теперь мы можем финализировать документацию и определить дальнейшие шаги.📄 Обновленный README.mdЭто окончательная версия вашего файла README.md, профессионально оформленная для демонстрации и развертывания.Markdown# 🛒 ShopList: Витрина товаров для розничной торговли

Сайт для локальных сетей розничной торговли, позволяющий публиковать ассортимент, цены и адреса магазинов, с реализованным контролем доступа по ролям (RBAC).

## 🚀 1. Стек технологий

* **Backend:** Django 5.2.7
* **Database:** PostgreSQL (Production), SQLite (Development)
* **Controlling Access:** Ролевая модель доступа (RBAC)
* **Hosting:** Production-ready (Gunicorn, WhiteNoise)
* **UI/Frontend:** Bootstrap 5 (Адаптивный дизайн)

## 📦 2. Установка и запуск проекта

### 2.1. Требования

* Python 3.10+
* Git

### 2.2. Установка

1.  **Клонирование репозитория:**
    ```bash
    git clone [https://github.com/ВАШ_НИК/shoplist.git](https://github.com/ВАШ_НИК/shoplist.git)
    cd shoplist
    ```

2.  **Создание и активация виртуального окружения:**
    ```bash
    python -m venv .venv
    # Windows
    .\.venv\Scripts\activate
    # macOS/Linux
    source .venv/bin/activate
    ```

3.  **Установка зависимостей:**
    ```bash
    pip install -r requirements.txt
    ```

4.  **Настройка переменных окружения:**
    Создайте файл `.env` в корне проекта для локальной работы:
    ```
    # .env
    SECRET_KEY='ваш_секретный_ключ'
    DEBUG=True
    ALLOWED_HOSTS=127.0.0.1,localhost
    ```

5.  **Миграции и создание тестовых данных:**
    ```bash
    python manage.py makemigrations
    python manage.py migrate
    
    # Создание тестовых пользователей для демонстрации ролей
    python manage.py shell 
    # (Далее вставьте код создания manager, user, admin из документации)
    # >>> [КОД ЗДЕСЬ]
    # >>> exit()
    ```

### 2.3. Запуск

```bash
python manage.py runserver
Проект будет доступен по адресу: http://127.0.0.1:8000/🔒 3. Тестирование Контроля Доступа (RBAC)Доступ к функциям создания, редактирования и удаления товаров контролируется ролями (@user_passes_test).РольЛогин / ПарольПрава на управление товарамиAdminadmin / testpasswordПолный доступ (Создание, Редактирование, Удаление)Sales Executivemanager / testpasswordПолный доступ (Создание, Редактирование, Удаление)Useruser / testpasswordТолько просмотр. Доступ к страницам управления запрещен (перенаправление 302).

Доступ к функциям создания, редактирования и удаления товаров контролируется ролями (@user_passes_test).РольЛогин / ПарольПрава на управление товарамиAdminadmin / testpasswordПолный доступ (Создание, Редактирование, Удаление)Sales Executivemanager / testpasswordПолный доступ (Создание, Редактирование, Удаление)Useruser / testpasswordТолько просмотр. Доступ к страницам управления

Тестирование проекта
python manage.py test